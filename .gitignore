# .NET
bin/
obj/
*.user
.vs/

# Secrets / local config
appsettings.Development.json
appsettings.Local.json
*.env
.env

# Media volume (local dev)
media/

# Local git worktrees (for testing PR branches; see test-worktree.sh)
worktrees/

# OS
.DS_Store

# Superpowers SDD scratch (briefs, reports, ledgers)
.superpowers/

# AI-assisted design docs (specs & plans) — kept local, not published
docs/superpowers/

# Node / Tailwind build (build-time only)
node_modules/
# Generated CSS bundle (built in Docker for prod, by `just dev` locally)
src/Mathom.Web/wwwroot/css/app.css
